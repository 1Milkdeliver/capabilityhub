interface:
  display_name: "My Skills"
  short_description: "Find and manage local Skills"
  default_prompt: "Use $myskills to show my live Skill inventory."
dependencies:
  tools:
    - type: "mcp"
      value: "capabilityhub-local"
      description: "Local CapabilityHub inventory and control runtime"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
