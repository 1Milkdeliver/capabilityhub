interface:
  display_name: "CapSift Help"
  short_description: "Localized capability help and live inventory"
  default_prompt: "Use $helpme to show the CapSift menu."
dependencies:
  tools:
    - type: "mcp"
      value: "capsift-local"
      description: "Local CapSift read-only inventory and search runtime"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
