interface:
  display_name: "CapabilityHub Help"
  short_description: "Localized capability help and live inventory"
  default_prompt: "Use $helpme to show the CapabilityHub menu."
dependencies:
  tools:
    - type: "mcp"
      value: "capabilityhub-local"
      description: "Local CapabilityHub inventory and control runtime"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
