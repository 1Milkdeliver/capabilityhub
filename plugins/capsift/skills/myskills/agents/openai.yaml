interface:
  display_name: "My Skills"
  short_description: "Find and inspect local Skills"
  default_prompt: "Use $myskills to show my live Skill inventory."
dependencies:
  tools:
    - type: "mcp"
      value: "capsift-local"
      description: "Local CapSift read-only inventory and search runtime"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
